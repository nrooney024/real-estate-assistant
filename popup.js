// chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
//     if (request.action === "receiveData") {
//       // Display manipulated data in the popup UI
//       document.getElementById("result").textContent = request.data;
//     }
// });